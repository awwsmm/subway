# frontend

## frontend-only development

Run the frontend with

```shell
npm run dev
```

Note that Vite uses HMR (hot module reloading), so you can make a change to the TypeScript code, save, and it will automatically appear in the browser.